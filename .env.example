# 复制本文件为 .env 并按实际环境填写。
# 安全加固：数据库密码不再硬编码在代码中，必须通过环境变量或 .env 提供。

# ===== 数据库配置 =====
DB_USER=root
DB_PASS=请填写你的数据库密码
DB_HOST=localhost
DB_PORT=3308
DB_NAME=file_scanner

# ===== 跨域来源（逗号分隔，按需增减）=====
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

@Entry
@Component
struct Help {
  build() {
    Column() {
      Text('使用帮助')
        .fontSize(20)
        .fontWeight(FontWeight.Bold)
        .margin({ top: 24, bottom: 16 })
        .fontColor($r('app.color.text_primary'))

      Scroll() {
        Column({ space: 14 }) {
          this.section('1. 开始扫描',
            '首页提供两种使用方式：\n方法一「开始扫描」：进入「扫描配置」列表，可新增配置（选择文件夹、文件类型、是否递归、排除文件夹、最小体积、扫描模式），或从已有配置直接开始扫描，完成后进入文库查看。\n方法二「一键清理」：选择目标文件夹后自动完成「扫描 → 解析 → 生成合集 → 勾选重复 → 清理删除」全流程，删除前可查看清单并二次确认。\n首页下方「更多功能」提供 帮助文档 / 操作日志 / 关键词替换 / 去重规则 的快捷入口。\n\n扫描配置支持两种扫描模式：「快速扫描」（不计算哈希，速度快）与「精确扫描」（计算内容 MD5 哈希，可识别内容相同但文件名不同的重复文件）。应用会递归读取文件，并在扫描时同步解析文件名提取书名/作者/进度/来源，用于识别重复。文件名解析已能正确处理作者名的修订后缀（如「作者：Nnnr（【修】山海镜花 融共同人）」会提取作者为 Nnnr，并正确识别来源、进度、书名）。扫描结果保存在本机，不会上传。')
          this.section('2. 查看文库与筛选',
            '扫描完成后进入文库，会按「每次扫描」分出多个文库。在文库内可按「全部 / 已勾选 / 未勾选 / 已标记 / 未标记 / 含已勾选」筛选，按时间、名称、大小排序。列表/合集两种视图通过顶部按钮切换。开启「勾选排前」后，已勾选项自动排到列表前部，方便核对。长按列表项可进入多选模式（全选 / 反选 / 勾选），点击文件可进入详情与内容预览。')
          this.section('2.1 搜索与拼音搜索',
            '支持按书名、作者、文件名搜索。更强大的拼音搜索：输入拼音全拼（如 doupo）或首字母缩写（如 dpcq），即可搜到「斗破苍穹」等中文书名/作者名，不用切换到中文输入法即可快速定位目标。')
          this.section('2.2 文件内容预览',
            '在文库列表或文件详情点击文件即可进入「文件预览」，直接查看 txt 文本内容。支持自动识别编码（UTF-8、UTF-8 BOM、GB18030、GBK）。点击预览页右上角「Aa」可展开字号面板，用 A- / A+ 调节显示字号。预览页提供可拖动的滚动条，拖动滑块即可快速上下（或左右，方向可在「我的 → 设置」中切换）滑动浏览；内容较多时采用流式分批加载并限制最大预览行数，避免大文件卡顿。可按需切换预览范围：前 50 行快速预览 / 全部内容 / 后 100 行（尾部）预览。')
          this.section('3. 分页浏览大库',
            '面对几万、十几万条数据，底部「分页导航条」帮助你逐页浏览：①「每页」输入框填每页条数，点「应用」生效；②「跳到」输入框填目标页码，点「跳转」直达该页；③「首页 / 上一页 / 下一页 / 末页」快速翻页。这样无需一次性加载全部数据，浏览更流畅。')
          this.section('4. 合集模式与合集筛选/排序',
            '合集模式按「书名」把同名文件聚合在一起，便于发现重复。点击右上角「更多」菜单选择「合集筛选」：可设置合集内文件数量区间（最少~最多，留空表示不限）以及需要排除的合集书名（多个用逗号分隔）。「合集排序」提供 8 种方式：数量多→少 / 少→多、大小大→小 / 小→大、名称 A→Z / Z→A、日期新→旧 / 旧→新。在合集视图中，每个合集的汇总卡片标题下会实时显示该合集「已勾选 X 个」，方便确认待删项的数量。')
          this.section('5. 勾选重复',
            '合集模式下点击「勾选重复」：在「同一文库内、按（作者 + 小说名）子分组」套用五则规则智能勾选待删重复项——① 完全相等去重：小说名+作者+大小+进度 四字段完全一致的多本中，保留最新一本，其余全部勾选；② 纯数字进度对比：同组内所有进度均为纯数字时，进度数字最大者不勾选，其余纯数字文件全部勾选；③ 含中文进度保护：进度含中文（如完结/连载）不勾选；③B 完结特例：同组若同时存在「文件名含完结/全本等字样」的文件与纯数字进度文件，当数字进度最大的文件体积小于所有完结字样文件中最小的那个，说明它不完整，会强制勾选删除，只留下完结版；④ 最大文件不勾选原则：同组内唯一文件大小最大者不勾选；⑤ 完结+N番外排序：进度匹配「完结+数字番外」（如「完结+3番外」）的文件，按数字 N 排序，最大者不勾选，其余强制勾选（但恰为本组体积最大者仍保护）。\n\n上述规则可在「首页 → 更多功能 → 去重规则」（或「我的 → 功能 → 勾选重复规则」）中单独开启或关闭（默认全部启用）。该页还支持自定义规则：选择字段（文件名/书名/作者/进度/来源站点/文件大小/创建时间）+ 操作符（等于/包含/正则匹配/大于/介于等）+ 值，多条件按 AND 组合，动作可选「勾选」或「保护」，并内置正则测试器可随时验证规则效果。')
          this.section('5.1 星标（标记）',
            '在文件详情页或文库列表可给文件加「星标」（标记），用于手动标记需要关注或保留的文件。星标不会触发删除，仅作为标识。「更多」菜单中的「按书名标重」可一键为同书名的重复文件加星标。通过「导出列表」可将当前筛选结果导出为 TXT 清单（可选择导出列、仅当前页或全部），用于备份待删列表。')
          this.section('5.2 关键词替换示例',
            '在「我的 → 功能 → 关键词替换规则」（或首页「更多功能 → 关键词替换」）中可配置清洗规则，分「扫描阶段」（清洗文件名）和「解析阶段」（清洗书名/作者等）。\n示例：① 文件名含「[www.xxx.com]」想清除，扫描阶段新增「匹配: [www.xxx.com]」「替换:（留空）」，扫描后该片段被删除；② 书名含「【校对版】」想去除，解析阶段新增「匹配: 【校对版】」「替换:（留空）」；③ 想把「第1章」统一为「第一章」，匹配「第(\\d+)章」替换对应中文数字（需用正则，自定义规则支持 op=正则匹配）。\n内置规则为系统预设（不可删除，仅可开关），自定义规则可自由增删，并支持「去掉/替换」批量新增：一次粘贴多行，「去掉」模式每行一个关键词（整行内容被移除）；「替换」模式每行用 || 分隔，如 AAA||B 表示把 AAA 替换成 B。')
          this.section('6. 一键清理',
            '首页点击「一键清理」：选择目标文件夹后，应用自动执行 5 个节点流水线：① 扫描文件 → ② 解析文件名 → ③ 生成合集 → ④ 按五则规则勾选重复 → ⑤ 清理删除。执行过程中可实时查看各节点进度与日志；到达「清理删除」节点前会展示复核清单，可全选 / 取消 / 勾选需要保留的文件，二次确认后才真正删除，避免误删。')
          this.section('7. 选择并删除',
            '勾选要删除的文件后，底部出现「删除」按钮，点击后先弹出「删除选择」窗口：【仅删除记录】或【删除记录和源文件（不可恢复）】；随后进入确认页，可全选 / 取消 / 勾选待删项，确认后由服务逐个删除并实时显示进度与日志。删除通过系统授权进行，请谨慎操作。建议删除前先通过「导出列表」备份待删清单（TXT，可选择列）。')
          this.section('8. 我的',
            '底部「我的」Tab 集中管理应用设置：外观（主题模式：跟随系统 / 浅色 / 深色；全局字号：小 / 标准 / 大；预览滚动条方向）、功能（关键词替换规则、勾选重复规则）、关于（操作日志、帮助、隐私协议、应用名称、版本、运行形态）。「操作日志」可查看扫描、解析、删除等历史操作记录。')
          this.section('9. 数据说明',
            '本应用纯本地运行，所有数据保存在应用私有目录，不上传任何服务器。删除文库仅清除数据库记录，不会删除原文件夹（除非在删除时选择「记录 + 源文件」）。所有扫描、解析、删除都在本机完成，数据与网络无关。')
          this.section('10. 权限与隐私',
            '本应用遵循最小权限原则，仅申请必要的文件访问权限：\n\n① 文件读取权限：通过系统 Picker（DocumentViewPicker）授权访问用户选择的文件夹，应用不会访问未授权的目录。\n② 文件写入/删除权限：仅在用户明确选择「删除记录和源文件」时，对已授权的文件执行删除操作。\n③ 数据存储：所有扫描结果、配置、规则均存储在应用私有沙箱目录，卸载应用后自动清除。\n④ 网络权限：本应用不需要网络权限，不会上传任何数据到服务器。\n\n权限持久化：授权的目录 URI 可通过 persistPermission 持久化，应用重启后通过 activatePermission 重新激活，无需重复授权。用户可随时在系统设置中撤销权限。\n\n隐私承诺：不收集任何用户数据，不包含任何第三方追踪 SDK，不含广告。')
        }
        .width('100%')
        .padding({ left: 16, right: 16, bottom: 24 })
      }
      .layoutWeight(1)
      .width('100%')
    }
    .width('100%')
    .height('100%')
    .backgroundColor($r('app.color.bg_page'))
  }

  @Builder
  section(title: string, body: string) {
    Column({ space: 6 }) {
      Text(title)
        .fontSize(16)
        .fontWeight(FontWeight.Medium)
        .fontColor($r('app.color.primary'))
      Text(body)
        .fontSize(14)
        .fontColor($r('app.color.text_secondary'))
        .lineHeight(20)
    }
    .alignItems(HorizontalAlign.Start)
    .width('100%')
  }
}
